--- libhdfs3-downstream-2.3.0.orig/libhdfs3/CMakeLists.txt	2018-01-09 16:39:34.000000000 -0200
+++ libhdfs3-downstream-2.3.0/libhdfs3/CMakeLists.txt	2018-08-04 09:12:38.989798154 -0300
@@ -20,19 +20,19 @@
 FIND_PACKAGE(Protobuf REQUIRED)
 FIND_PACKAGE(KERBEROS REQUIRED)
 FIND_PACKAGE(GSasl REQUIRED)
-FIND_PACKAGE(GoogleTest REQUIRED)
+# FIND_PACKAGE(GoogleTest REQUIRED)
 FIND_PACKAGE(SSL REQUIRED)
 FIND_PACKAGE(CURL REQUIRED)
-INCLUDE_DIRECTORIES(${GoogleTest_INCLUDE_DIR})
-LINK_LIBRARIES(${GoogleTest_LIBRARIES})
+# INCLUDE_DIRECTORIES(${GoogleTest_INCLUDE_DIR})
+# LINK_LIBRARIES(${GoogleTest_LIBRARIES})
 
 IF(OS_LINUX)
     FIND_PACKAGE(LibUUID REQUIRED)
 ENDIF(OS_LINUX)
 
-ADD_SUBDIRECTORY(mock)
+# ADD_SUBDIRECTORY(mock)
 ADD_SUBDIRECTORY(src)
-ADD_SUBDIRECTORY(test)
+# ADD_SUBDIRECTORY(test)
 
 CONFIGURE_FILE(src/libhdfs3.pc.in ${CMAKE_SOURCE_DIR}/src/libhdfs3.pc @ONLY)
 CONFIGURE_FILE(debian/changelog.in ${CMAKE_SOURCE_DIR}/debian/changelog @ONLY)
